# Portfolio 2026 Audit — Revision Tracker

Branch: `audit-2026-revisions`

This file pairs with the `AUDIT 2026` HTML comments left inline in the markup. Every change below was
written in your voice and is either **already true** or **a truthful retelling of work you actually did**
(BAM / DoD, or your own upstart projects). Nothing claims employment anywhere you haven't worked.

Items marked **⚠ SUBSTANTIATE** are aspirational — the copy is live so you can see the target, but you
need to do the real work before it should go public. Each says exactly what "done" looks like.

---

## Homepage (`index.html`)

### 1. Hero subhead — reframed toward horizontal / agentic / design-to-code
Old: "...complex SaaS through token-based design systems, AI-assisted design and development..."
New: "...complex B2B SaaS — designing across product areas with semantic token systems, agentic AI
workflows, and shipping design-to-code with no hand-off."

- ✅ "design-to-code, no hand-off" — true (you coded the Profile page on the Nav project; built Halfway/StellarCut).
- ⚠ SUBSTANTIATE "across product areas" (horizontal): every current case study is single-surface. You need
  one story that spans **more than one product area or team**. (Stripe's exact ask: "horizontal projects
  spanning multiple organizations and product teams.")
- ⚠ SUBSTANTIATE "agentic AI workflows": today that's the StellarCut human-in-the-loop story reframed (good),
  but one project is thin. Consider a second agentic example.

### 2. NEW "How I Work" tools section
Honest split: **In my stack** (Figma, Lovable, Git/GitHub, Node, HTML/CSS/Tailwind, ElevenLabs, Netlify)
vs **Adopting for 2026** (Cursor, Claude Code, v0, React/Next.js, Style Dictionary, Tokens Studio).

To move a chip from "Adopting" → "In my stack" (this is the audit's #1 tech-stack gap):
- ⚠ **Cursor / Claude Code** — rebuild or extend one shipped project in a real dev toolchain; add a sentence of proof.
- ⚠ **v0** — generate and ship one UI from a v0 prototype; screenshot it in a case study.
- ⚠ **React / Next.js** — port Halfway (or a new build) to Next.js; link the repo.
- ⚠ **Style Dictionary / Tokens Studio** — export your design system's tokens and show the output.

### 3. NEW "Design Systems & Tokens" block
The copy is true (you co-managed the 200-component system at BAM). The audit's point: it's *claimed but
never shown*. The three cards are scaffolds with placeholder image boxes.
- ⚠ SUBSTANTIATE: replace the 3 placeholder boxes with real artifacts —
  (a) a primitives → semantic token map, (b) a Figma variables screenshot, (c) a component documented
  against its tokens — plus a real link (Figma library / Storybook / token file). Keep specifics generic
  if NDA-bound, like the Nav case study does.

### 4. Resume Builder card description — de-fluffed
Old: "...revamped into an easy-to-use product via UI and visual design best practices."
New: leads with the ~2 min → ~1 sec outcome. ✅ true.

---

## StellarCut (`works/stellarcut.html`)

### 5. "Why Human-in-the-Loop?" → "The Agentic Design Decision"
Reframed the section to name the agentic-UX thinking (what the agent automates vs. what the human keeps),
plus a two-column "agent handles / human decides" breakdown. ✅ Truthful to what you built.
- ⚠ OPTIONAL deepen: add a screenshot of a moment you kept the human in control (AI suggests, user confirms).
- Note: tool-stack still shows "Lovable.dev" (true). The homepage "Adopting" section carries the Cursor/Claude Code gap.

---

## Enterprise App Navigation (`works/enterpriseAppNavigation.html`)

### 6. Overview — elevated from "card sort" to strategic IA program
Reframed to lead with restructuring a 400+ page IA under technical constraints + driving a research program.
Card sort is now a detail inside the strategy, not the headline. ✅ Truthful retelling.

### 7. Final step — named the "no hand-off"
Heading: "I Directed the Redesign — Then Shipped Part of It Myself." Copy names pairing with engineering and
personally coding the Profile page (Linear's "no hand-offs"). ✅ true — same work, named for 2026.

---

## Resume Builder (`works/resumeBuilder.html`)

### 8. Dropped the "Don't Make Me Think" citation
Kept the principle (minimalism as constraint), removed the named book reference the audit flagged as UX-101. ✅

### 9. Closing — leads with constraint + performance outcome
Reframed the "best practices" filler to lead with the 2-minute generate/download constraint and the
~2 min → ~1 sec result. Also fixed the "buildning" typo. ✅ true.

### 10. "Progressive disclosure" header — de-jargoned
"Closed accordions allow for progressive disclosure..." → "One step at a time, instead of everything at once." ✅

---

## Biggest remaining gap (not yet addressed in this pass)

**A horizontal / system-level case study.** The audit's clearest miss is that every case study is a single
surface. The highest-value next move is one case study (truthful retelling from BAM/DoD, or a new upstart
project) that shows you influencing design direction **across a whole platform or product ecosystem** —
multiple teams/surfaces, not one screen. Everything else above is reframing; this one needs new work.
